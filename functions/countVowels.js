const string = require('../string');

// Complete this function to count the number of vowels in a given string. Vowels are the letters 'a', 'e', 'i', 'o', and 'u'.

/**
 * Counts the number of vowels in a given string.
 *
 * @param {string} string - The input string to count vowels from.
 * @returns {number} - The number of vowels in the given string.
 *
 * @example
 * const text = "Hello, World!";
 * const result = countVowels(text);
 * // Result: 3 (vowels are 'e', 'o', and 'o')
 */
const countVowels = (string) => {};

console.log(`Le nombre de voyelles: ${countVowels(string)}`);

module.exports = countVowels;
